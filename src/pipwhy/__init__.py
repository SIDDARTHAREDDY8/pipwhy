"""pipwhy — pip's dependency conflicts, explained.

Parses pip's ``ResolutionImpossible`` error output and tells you which
requirement is actually blocking the install, instead of making you read the
resolver's raw backtracking dump.
"""

__version__ = "0.1.0"
